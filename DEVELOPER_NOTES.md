# Developer Notes (for my own reference)

## Create a tag for a release

```text
git tag -a v<VERSION> -m "<MESSAGE>"
```

```text
git push origin v<VERSION>
```

**Make sure to create a release after creating a tag.**
